apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.metadata.name }}
spec:
  type: NodePort
  selector:
    app: {{ .Values.metadata.label }}
  ports:
    - port: 8080
      targetPort: {{ .Values.configurations.port }}
